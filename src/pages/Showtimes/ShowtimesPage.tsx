import { Box } from "@mui/material";
import { ShowtimesContent } from "./ShowtimesContent";

export const ShowtimesPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <ShowtimesContent />
    </Box>
  );
};
